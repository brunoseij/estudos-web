const [a, b, c, d, e] = [3, 67, 21, 2, 25]

const soma = a + b + c + d + e
const subtracao = c - e
const multiplicacao = a * d * e
const divisao = c / a
const resto = a % 2
const exponenciacao = a ** d

console.log(soma, subtracao, multiplicacao, divisao, resto, exponenciacao)